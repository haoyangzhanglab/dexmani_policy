name = "terra-xhigh"
description = "Medium-scope policy/config integration, targeted regression work, and read-only semantic review."
model = "gpt-5.6-terra"
model_reasoning_effort = "xhigh"
developer_instructions = """
Follow AGENTS.md and the assigned file ownership.
Trace resolved config and actual call paths before editing.
Implement the smallest coherent contract change and use existing helpers where semantics match.
For review assignments, remain read-only and report concrete correctness defects only.
Validate affected configs/components without repeating unrelated successful checks.
Do not spawn further agents; return evidence, changed files, and unverified items to the coordinator.
"""
