# @package _global_

defaults:
  - dataset: multitask_rgb
  - _self_

policy_name: multitask_dit

task_name: multitask_dit
seed: 42

horizon: 16
n_obs_steps: 2
n_action_steps: 8
action_dim: 19

dataloader:
  batch_size: 64
  num_workers: 8
  shuffle: true
  pin_memory: true
  persistent_workers: false

val_dataloader:
  batch_size: 64
  num_workers: 4
  shuffle: false
  pin_memory: true
  persistent_workers: false

agent:
  _target_: dexmani_policy.agents.core.multi_task.MultiTaskAgent
  text_encoder_model: openai/clip-vit-base-patch16
  task_texts: ${dataset.task_texts}
  rgb_backbone_name: dino
  rgb_backbone_config:
    tune_mode: lora  # freeze | full | lora
  state_dim: 19
  state_out_dim: 64

  # backbone (DiT)
  n_emb: 512
  num_heads: 8
  n_layers: 6
  mlp_ratio: 4.0
  qkv_bias: true

  # action decoder (Diffusion)
  num_training_steps: 100
  num_inference_steps: 10
  prediction_type: sample

  horizon: ${horizon}
  n_obs_steps: ${n_obs_steps}
  n_action_steps: ${n_action_steps}
  action_dim: ${action_dim}

optimizer:
  lr: 1.0e-4
  weight_decay: 1.0e-6
  obs_lr: 1.0e-4
  obs_weight_decay: 1.0e-6
  betas: [0.95, 0.999]

ema:
  _target_: dexmani_policy.training.common.ema_model.EMAModel
  update_after_step: 0
  inv_gamma: 1.0
  power: 0.75
  min_value: 0.0
  max_value: 0.9999

workspace:
  _target_: dexmani_policy.training.common.workspace.TrainWorkspace
  output_dir: ${hydra:runtime.output_dir}

  wandb_cfg:
    _target_: dexmani_policy.training.common.workspace.WandbConfig
    project: dexmani_policy
    group: ${policy_name}
    name: ${task_name}_${now:%Y-%m-%d_%H-%M}_${training.seed}
    id: ${policy_name}_${task_name}_${training.seed}
    resume: allow
    mode: offline
    video_fps: ${env_runner.env_video_fps}

  checkpoint_cfg:
    _target_: dexmani_policy.training.common.workspace.CheckpointConfig
    monitor_key: test_mean_score
    mode: max
    topk: 3

training:
  seed: ${seed}
  device: cuda:0
  use_ema: true
  use_ema_teacher_for_consistency: false
  lr_scheduler: cosine
  lr_warmup_steps: 500
  loop:
    num_epochs: 1000
    log_interval_steps: 50
    val_interval_epochs: 25
    eval_interval_epochs: 250
    sample_interval_epochs: 5
    grad_accum_steps: 1
    grad_clip_norm: 1.0

env_runner:
  _target_: dexmani_policy.env_runner.multi_task_sim_runner.MultiTaskSimRunner
  n_obs_steps: ${n_obs_steps}
  env_video_fps: 10
  default_eval_episodes: 20
  sensor_modalities: ${dataset.datasets[0].sensor_modalities}
  task_configs:
    - task_name: task_1
      task_text: ${dataset.task_texts[0]}
    - task_name: task_2
      task_text: ${dataset.task_texts[1]}
    - task_name: task_3
      task_text: ${dataset.task_texts[2]}

# ---- offline eval (eval_sim.py)
eval:
  seed: 0
  offline:
    ckpt_tag_or_path: best
    eval_episodes: 100
    denoise_timesteps_list: [10]
    use_ema_for_eval: true

hydra:
  job:
    override_dirname: ${policy_name}_${task_name}
  run:
    dir: experiments/${policy_name}/${task_name}/${now:%Y-%m-%d_%H-%M}_${training.seed}
  sweep:
    dir: experiments/${policy_name}/${task_name}/${now:%Y-%m-%d_%H-%M}_${training.seed}
    subdir: ${hydra.job.num}
