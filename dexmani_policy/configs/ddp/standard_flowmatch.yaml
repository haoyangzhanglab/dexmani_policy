# @package _global_
# Standard FlowMatch — DDP multi-GPU training overlay
# Usage: bash scripts/training/train_ddp.sh ddp/standard_flowmatch

defaults:
  - /standard_flowmatch
  - _self_

policy_name: ddp/standard_flowmatch

training:
  num_gpus: 4
  gpu_ids: null

dataloader:
  batch_size: 32
  num_workers: 4

val_dataloader:
  batch_size: 32
  num_workers: 4

hydra:
  job:
    override_dirname: ${policy_name}_${task_name}
  run:
    dir: experiments/${policy_name}/${task_name}/${now:%Y-%m-%d_%H-%M}_${training.seed}
  sweep:
    dir: experiments/${policy_name}/${task_name}/${now:%Y-%m-%d_%H-%M}_${training.seed}
    subdir: ${hydra.job.num}
