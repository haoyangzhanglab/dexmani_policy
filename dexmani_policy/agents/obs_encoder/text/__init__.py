"""Text encoders (CLIP / T5).

Import encoders directly from their modules (e.g.
``from ...text.clip import CLIPTextEncoder``); this package does not re-export
them.  ``T5TextEncoder`` is a reserved component — import it directly if needed.
"""
