# 修复执行工作清单

> 基线：`cebe2b4`（2026-09-02）。每条结论已回到当前 HEAD 做对抗式核验（10 条，8 CONFIRMED / 2 PARTIALLY）。
> 来源：`docs/全仓审查问题优先级与修复建议.md`（正确性）+ `docs/冗余机制Fact-check与删除收益排序.md`（可维护性）+ 本轮核验。
> 目的：把两份文档各自独立的 Phase 计划合并成**单一执行顺序**，标出决策点、依赖和门禁。

## 0. 决策点（先拍板，gate 下面工作包）

| # | 决策 | 选项 | 影响范围 |
|---|---|---|---|
| D1 | MultiTask 留还是归档 | **留** / 归档 | A2、C6 是否执行 |
| D2 | R3M 旧 checkpoint 策略 | ~~保留 legacy 推理 + 告警~~ / **只 official_v1（已决，2026-09-02）** | A1 兼容层形态 |
| D3 | selector 修还是换 | 修 598 行 / **换固定 seed + 固定 policy RNG 短实现** | A4 形态 |
| D4 | 预训练权重 fail-closed 默认 | `allow_random_init` 默认值（建议默认 false） | B5.6 |

> 推荐：D1=留（先修统计再决定归档）、D2=~~保留 legacy 推理~~ **只 official_v1**（仓库无任何旧 moe_dp/R3M checkpoint，legacy 无保护对象，已删除）、D3=换（一次性解决 3 条 correctness 风险 + 过度工程化）、D4=false。

## 1. 工作包总览

| ID | 优先级 | 工作包 | 依赖 | 风险 |
|---|---|---|---|---|
| C1 | 删冗余 | 删 Top-K 状态机 | — | 低 |
| C2 | 删冗余 | 迁移+删内联 example/self-test | — | 低 |
| C3 | 删冗余 | 删 Trainer 死验证/死 eval + 16 个 val 配置 | — | 低 |
| C4 | 删冗余 | barrel import 改直接/惰性导入 | — | 低 |
| C5 | 删冗余 | 删 5 个零调用 helper | — | 低 |
| A1 | P0 | R3M 预处理版本化 | D2 | 中 | ✅ 已完成 |
| A3 | P1 | eval 异常分类 + 端到端 fail-fast | — | 中 |
| A4 | P1/P2 | selector 拒绝无效候选 | D3, A3 | 中 |
| A2 | P0 | MultiTask 统计重写 | D1 | 中高 |
| B1 | P1 | checkpoint 恢复语义 | — | 中高 |
| B2 | P1 | DDP 恢复顺序 + 尾批 | — | 中 |
| B3 | P1 | parity 通过后再发布 | — | 中 |
| B4 | P1 | 远程脚本参数白名单 | — | 中 |
| W4 | 条件 | dormant 组件归档（文档二 Phase 2-4） | 研究决策 | 高 |

## 2. Wave 1 — 低风险纯死代码 + 独立 P0（可并行）

### C1 · 删 Top-K 状态机
- **核验修正**：文档二说"无调用方"不准确 —— `workspace.py:46` 每次构建 TrainWorkspace 都实例化 `TopKCheckpointTracker`，`CheckpointConfig` 被 9 份配置 Hydra 实例化。真正零调用的是 `save_topk()`（`workspace.py:93`）。定性应为 **wired-but-never-fired**，删除结论不变。
- 文件：`common/checkpoint_io.py:129-243`（`TopKCheckpointTracker`）、`training/workspace.py`（`CheckpointConfig`/`topk_tracker`/`save_topk`）、9 份 config 的 `checkpoint_cfg` 块。
- 改动：删 tracker/config/`save_topk()`；`resolve_checkpoint_path()` 只留 `latest`/显式路径；暂保留 `state.monitor` 宽松读取兼容旧 `simple.v1` checkpoint。
- **不删**：`CheckpointStore`、临时文件 + atomic replace、`latest.pt`。
- 门禁：milestone 原子写 / `latest.pt` 原子替换 / interrupt resume / 离线 best 解析仍通过。

### C2 · 迁移+删内联 example/self-test（约 1,626 行）
- 文件：`core/moe.py`(213)、`plugins/moe.py`(137)、`action_flow_dit.py`(103)、`token_compressor.py`(95)、`pointnext_tokenizer.py`(74)、`maniflow.py`(73) 等 23 个 `example()`/`_example()` + 5 个 raw `__main__`。
- 改动：只迁有价值断言（MoE aux-loss / ActionFlow shape-cache / backbone shape）到 `tests/`；删 `example()`、打印参数量、随机 tensor 演示、重复 shape 输出。
- **保留**：真正 CLI 的 `__main__` guard（train/eval/export/tools）。

### C3 · 删 Trainer 死验证/死 eval
- 文件：`training/trainer.py`（`validate()` :336、`evaluate()` :391、`max_val_steps` :31/365）、`train.py:59,68`（`val_loader=None`/`env_runner=None`）、16 个 `val_dataloader` 配置块。
- 改动：删 `TrainingComponents.val_loader/env_runner` 构造、Trainer 两方法、`max_val_steps`、16 个 `val_dataloader` 块。
- **保留**：各主配置的 `env_runner`（离线评测经 `training/eval_utils.py` 实例化）。

### C4 · barrel import 改直接/惰性导入
- **核验修正**：文档二说"同包其他实现全部导入"过度 —— `pointcloud/__init__.py` 漏 `geoformer.py`、`plugins/__init__.py` 漏 `moe.py`、`core/__init__.py` 漏 `base.py`（这三处已是直接导入）。eager + 拉重依赖（torchvision/transformers/pytorch3d）属实。
- 文件：5 个 `__init__.py`；内部使用方 `core/dp.py`/`core/moe.py`（从 RGB barrel 导入）、`pointcloud/registry.py`（从 pointcloud barrel 导入）改具体模块导入。
- 改动：先清内部使用方，再清空或 lazy export；加逐策略 import smoke。

### C5 · 删 5 个零调用 helper（75 行）
- 文件：`training/eval_utils.py:168` `resolve_best_checkpoint`、`obs_encoder/pointcloud/ops.py:144` `square_distance`、`deployment/restore.py:374` `compare_prediction_snapshots`、`action_decoders/time_sampler.py:8` `logit_normal_density`、`agents/optim_util.py:6` `get_default_optim_group`。
- 注意：删 `resolve_best_checkpoint` 时同步修 `docs/仿真评测机制.md:89` 的图引用；**不要**误删在用的 `resolve_checkpoint_path`。

### A1 · R3M 预处理版本化（P0）— ✅ 已完成（2026-09-02）
- **最终形态**：经 D2 反转（无旧 moe_dp/R3M checkpoint 需保护）后，未保留 `legacy_255` 兼容层，直接改为「只 official_v1」——`r3m.py` 的 `forward` 一律 `[0,1] → ImageNet norm`（删除了错误的 `mul(255.0)`），不再有 `preprocess_version` 参数与告警逻辑。
- 文件：`agents/obs_encoder/rgb/r3m.py`（`forward` 删除 `mul(255.0)`）、`configs/moe_dp.yaml`（无字段变更，行为随代码修正）。
- 根因确认（对照官方源码 `facebookresearch/r3m` `models_r3m.py:96-99`）：官方 forward 收 uint8 `[0,255]` → `/255` → `[0,1]` → ImageNet norm，等价于对 `[0,1]` 直接 normalize，**无 ×255**。旧注释「trained with [0,255]」是误解。
- 验收：`tests/test_r3m_preprocess.py` 位级验证 `R3M(x) == convnet((x-mean)/std)` 且 `R3M(x) != convnet((255x-mean)/std)`。
- 回归：`docs/全仓审查...` §9「R3M preprocessing parity」。

## 3. Wave 2 — 评测正确性

### A3 · eval 异常分类 + 端到端 fail-fast（P1）
- 文件：`env_runner/base_runner.py:300-308`（`except Exception` 分类）、`eval_best_ckpt.py:463-471`（`--denoise-steps` 预校验）、`action_flow_flowmatch.py`（`_resolve_nfe` 前置到构造/入口）。
- 改动：拆三类异常 —— ContractError/NFE/shape/OOM/模型 forward → **上抛非零退出**；环境正常结束的任务失败 → `success=False`；视频编码错误与 episode 指标分离；episode 前预校验 `denoise_timesteps_list`/episodes/seed pool/action slice。
- 注意：`_resolve_nfe` 当前在 `action_flow_flowmatch.py:71-79` 只校验 config 默认 `denoise_steps`，`--denoise-steps` override 不走构造校验 → 需在 `eval_best_ckpt.py` 入口加同款 midpoint 偶数校验。
- 验收：midpoint NFE=3 在 episode 前非零退出、不写 0%；注入 OOM/模型异常 selector 不写 `best_ckpt.json`；真任务失败仍 `success=False`。

### A4 · selector 拒绝无效候选（gate D3）
- 若**修**：`select_best_ckpt.py` 异常不转 0 分而是 `invalid`；候选要求相同非零有效样本数；`invalid` 不参与 best。
- 若**换**（推荐）：固定 25/50 seeds；每 `(checkpoint, seed)` 恢复相同 policy RNG；load/model/CUDA 异常整体失败；排序 `success_rate → 成功 episode avg_steps → global_step`。
- 针对 3 条已核验风险：`:286-289` 异常→空 accumulator、`:183` 全失败按 `global_step` 决胜、`:351-352` Phase2 batch 失败导致不同分母。
- 验收：全失败不写 `best_ckpt.json`；同 seed 同 policy noise；不同分母不再发生。

### A2 · MultiTask 统计重写（P0，gate D1=留）
- **张力厘清**：文档二 §6 撤回的"runner 签名不兼容 / 空 rates-steps 除零"属实（`run()` 已兼容 4 参数、`:167-168` 已防除零），但**只覆盖 run() 层**；文档一 P0 的 `get_seed_list` 缺失 / 分母>1 / macro 漏算仍在 eval 入口层全部成立，**未被推翻**。
- 文件：`env_runner/multi_task_sim_runner.py:25`（加 `get_seed_list` 或入口分支）、`eval_best_ckpt.py:105/146/156-157`（`_select_eval_seeds` 无条件调 `get_seed_list`、分母 `len(eval_seeds)`）、`training/eval_utils.py:229-238`（`collect_episode_details` 跨任务展平）。
- 改动：评测单位 = `(task, seed)`；输出 per_task SR + macro + micro + 有效/无效 episode 数；micro 分母 = 实际有效 episode 数；macro 不静默省略失败 task；单 task 基础设施失败 → 整体 `invalid` + 非零退出。
- 验收：2 task × 3 seed = 6 episode 单元、micro 分母=6；所有 SR ∈ [0,1]；单任务 runner 统计不受影响。

## 4. Wave 3 — 长训练/发布（P1，进长期训练/发布前完成）

- **B1 恢复语义**：显式 `resume_from=<dir|ckpt>`（不跨时间戳猜）；保存 EMA counter、Python/NumPy/Torch/CUDA RNG、scheduler、epoch/batch 边界；milestone/interrupt 只在完成 optimizer step 后发布；W&B id 由 experiment identity 派生。
- **B2 DDP**：恢复顺序 = 构造 optimizer → scheduler → load optimizer state → load scheduler state；尾批显式 flush 或 `drop_last` 校验（单卡/DDP 一致）；`clip_grad_norm_` 非 finite 外 RuntimeError 上抛；debug ckpt 由真检测 rank 提供证据。
- **B3 parity 后发布**：exporter `publish=False` 或写临时目录；parity/sidecar 校验全过后只做一次原子 selector 更新；parity 失败清理候选、selector 不变、清理 pre-selector orphan 使重试幂等。
- **B4 远程脚本**：`--gpus` 仅 `^[0-9]+(,[0-9]+)*$` 且拒重复/越界；policy/task/timestamp/session 白名单；`SUBPATH` 拒绝对路径/`..`/空组件/符号链接；tmux 仓库专属前缀、`--all` 只操作该集合；未知参数立即退出、`--dry-run` 拼错不退化为真实执行。

## 5. Wave 4 — 条件归档（研究决策后，一次只归档一个子系统）

由 owner 明确未来 1-2 个实验周期的 active set 后依次处理：MoE、MultiTask（若 D1=归档）、DDP、RGB-D、R3D aux-EE、预留组件（CLIP Vision/SigLIP/PointNext global/T5/TokenCompressor）。每次记录最后可恢复 commit。

## 6. 回归测试映射（文档一 §9 → 工作包）

| 测试 | 保护的工作包 |
|---|---|
| R3M preprocessing parity / legacy 不静默换模式 | A1 |
| MultiTask micro/macro 分母正确、SR≤1、invalid 不漏 | A2 |
| Invalid NFE CLI episode 前非零退出、不写 0%/best | A3 |
| Runner exception taxonomy（OOM 上抛 / 真失败计数） | A3 |
| selector 全失败不写 best_ckpt.json / 同 seed 同 noise | A4 |
| Resume equivalence（LR/EMA decay/global step/样本边界） | B1 |
| DDP resume 首个 LR 正确 | B2 |
| Accumulation 尾批显式 flush 或显式拒绝 | B2 |
| Qualification rollback（parity 失败 selector hash 不变） | B3 |
| Remote script 对抗参数拒绝 | B4 |
| Paired NFE 同一 seed 同 policy noise | A4 |

## 7. 执行顺序 + 门禁

```
Wave 0  拍板 D1-D4
Wave 1  C1→C5（纯死代码，低风险）+ A1（R3M，独立 P0）
        门禁: compileall + 逐策略 import + checkpoint latest/resume 单测 + 现有 83 test method（允许环境 skip）
Wave 2  A3 → A4 → A2
        门禁: 同 checkpoint 同 seeds 新旧结果对照；故障注入中止不产 0%；best/latest/deploy 不互相 fallback
Wave 3  B1-B4（进长期训练/发布前）
        门禁: 中断恢复不静默改状态；parity 失败不切 selector
Wave 4  dormant 归档（研究决策后逐个）
```

## 8. 红线（不要误删/误修）

- **normalizer 全量 replay buffer 拟合是有意设计**，不是 bug，不要偷偷改成 train-only。
- **ActionFlow 独立实现**（`action_flow_flowmatch.py`/`action_flow_dit.py`/`geoformer.py`）不要与 legacy flow/DiT 合并。
- **不删**：checkpoint atomic replace / deployment strict restore+hash+parity+原子 selector / ActionFlow KV cache 契约 / 当前 canonical 的 DINO/R3M/ResNet/Uni3D/PointNext patch tokenizer。
- TokenCompressor、T5TextEncoder 是**明确预留组件**，只能走 W4 路线决策，不能当死代码删。
- MoE 若保留：先改 README"稀疏不增 FLOPs"表述（当前 `aggregate_experts()` 先算全部 active experts 再 gather top-k，是 dense 不是 compute-sparse）。
