# DexMani_Policy 架构全景文档

> **文档导航**: [README](../README.md) — 项目概览 · [CLAUDE.md](../CLAUDE.md) — AI 工作速查 · [评测机制](评测机制.md) — 评测全链路 · [SSH 部署](SSH服务器训练部署.md) — 远程训练

---

## 1. 完整目录树（带注释）

```
DexMani_Policy/
├── CLAUDE.md                                  # 项目 AI 助手指令（速查、FAQ、设计约定）
├── dexmani_policy/                            # 核心 Python 包
│   ├── agents/                                # Agent 模块（模型主体）
│   │   ├── __init__.py                        # 空
│   │   ├── optim_util.py                      # 优化器参数组工具（OptimGroupMixin、分组函数）
│   │   ├── position_encodings.py              # 位置编码（SinusoidalPosEmb, TimestepMLP, 3D PE）
│   │   ├── core/                              # Agent 具体实现
│   │   │   ├── __init__.py                    # 导出 7 个 Agent 类
│   │   │   ├── base.py                        # BaseAgent + UNetDiffusionAgent + DiTXFlowMatchAgent
│   │   │   ├── dp.py                          # DPAgent（RGB+DINO/CLIP/SigLIP/ResNet/R3M+UNetDiffusion）
│   │   │   ├── dp3.py                         # DP3Agent（PointCloud+iDP3/PointNeXT+UNetDiffusion）
│   │   │   ├── maniflow.py                    # ManiFlowAgent（PC tokenizer+DiTX+FlowMatchWithConsistency）
│   │   │   ├── moe.py                         # MoEAgent（16专家 MoE+UNetDiffusion）
│   │   │   ├── multi_task.py                  # MultiTaskAgent（RGB+CLIP Text+DiT+Diffusion/FlowMatch）
│   │   │   ├── r3d.py                         # R3DAgent（Uni3D+OneWayTransformer+Diffusion）
│   │   │   └── dqrise.py                      # DQRISEAgent（VQ-VAE码本+缩减动作空间Diffusion）
│   │   ├── action_decoders/                   # 动作解码器
│   │   │   ├── __init__.py                    # 空
│   │   │   ├── time_sampler.py                # 时间采样器 TimeSampler（flow matching 用）
│   │   │   ├── diffusion.py                   # Diffusion（DDIM 包装器）
│   │   │   ├── flowmatch.py                   # FlowMatch + FlowMatchWithConsistency
│   │   │   └── backbone/                      # 解码器 Backbone
│   │   │       ├── __init__.py                # 空
│   │   │       ├── unet1d.py                  # ConditionalUnet1D（FiLM 条件化 UNet）
│   │   │       ├── dit.py                     # DiTDiffusion（AdaLN 调制 Transformer）
│   │   │       ├── ditx.py                    # DiTXFlowMatch / DiTXDiffusion（Cross-Attention Transformer）
│   │   │       └── one_way_transformer.py     # OneWayTransformer（R3D 专用 Cross-Attention）
│   │   ├── obs_encoder/                       # 观测编码器
│   │   │   ├── __init__.py                    # 空
│   │   │   ├── plugins/                       # 编码器插件
│   │   │   │   ├── __init__.py                # 导出 TokenCompressor、ModulatedTokenCompressor、TopKReducer
│   │   │   │   ├── moe.py                     # MoE（MoE 门控+负载均衡 aux loss）
│   │   │   │   └── token_compressor.py        # Token 压缩器（未启用）
│   │   │   ├── proprio/                       # 本体感知编码器
│   │   │   │   ├── __init__.py                # 导出 StateMLP
│   │   │   │   └── state_mlp.py               # StateMLP（hidden=[64]）
│   │   │   ├── pointcloud/                    # 点云编码器
│   │   │   │   ├── __init__.py                # 全量导出
│   │   │   │   ├── ops.py                     # FPS、ball-query、k-NN、分组、采样
│   │   │   │   ├── registry.py                # PC 编码器注册表（工厂函数+默认配置）
│   │   │   │   ├── pointnet.py                # PointNet、MultiStagePointNet
│   │   │   │   ├── pointnext.py               # PointNextEncoder（层级 SA+IRPB）
│   │   │   │   ├── pointnext_tokenizer.py     # PointNextPatchTokenizer（多尺度 patch）
│   │   │   │   ├── uni3d.py                   # Uni3DPointcloudEncoder（FPS+k-NN+ViT+Fourier PE）
│   │   │   │   └── r3d_obs_encoder.py         # R3DObsEncoder（Uni3D+StateMLP+沿特征维拼接）
│   │   │   ├── rgb/                           # RGB 图像编码器
│   │   │   │   ├── __init__.py                # 全量导出
│   │   │   │   ├── base.py                    # ViTEncoder 基类（forward/backproject/LoRA）
│   │   │   │   ├── registry.py                # RGB Backbone 注册表（工厂函数+预设配置）
│   │   │   │   ├── types.py                   # 类型别名（TuneMode, GlobalTokenType, NormMode）
│   │   │   │   ├── utils.py                   # 张量工具（flatten/restore batch、内参更新）
│   │   │   │   ├── image_processor.py         # ImageProcessor（resize/crop/normalize）
│   │   │   │   ├── geometry_processor.py      # GeometryProcessor（深度反投影+patch坐标池化）
│   │   │   │   ├── clip.py                    # CLIP ViT 编码器
│   │   │   │   ├── dino.py                    # DINOv2 ViT 编码器
│   │   │   │   ├── siglip.py                  # SigLIP ViT 编码器（无 CLS，pooler 全局 token）
│   │   │   │   ├── resnet.py                  # ResNet CNN（FrozenBN2d、GN 转换、backproject）
│   │   │   │   └── r3m.py                     # R3M ResNet（Ego4D 预训练加载器）
│   │   │   └── text/                          # 文本编码器
│   │   │       ├── __init__.py                # 导出 CLIPTextEncoder、T5TextEncoder
│   │   │       ├── clip.py                    # CLIPTextEncoder（冻结 CLIP text backbone）
│   │   │       └── t5.py                      # T5TextEncoder（预留，未使用）
│   │   └── vq_hand/                           # DQ-RISE VQ-VAE 手势量化
│   │       ├── __init__.py                    # 全量导出
│   │       ├── vqvae.py                       # VQVAEHand + EncoderMLP
│   │       ├── vector_quantize.py             # EuclideanCodebook + VectorQuantize（EMA+K-means+死码过期）
│   │       ├── residual_vq.py                 # ResidualVQ（级联 VQ+softmax 加权）
│   │       └── codebook_manager.py            # CodebookManager（PCA 排序+运行时查找）
│   ├── common/                                # 通用工具
│   │   ├── __init__.py                        # 空
│   │   ├── config.py                          # Hydra 配置解析器（register_resolvers, validate）
│   │   ├── normalizer.py                      # LinearNormalizer + 辅助函数
│   │   ├── pytorch_util.py                    # 通用 PyTorch 工具（seed, dict_apply, MLP 工厂）
│   │   ├── checkpoint_io.py                   # TrainCheckpoint + CheckpointStore + TopK 追踪器
│   │   ├── faas_mapper.py                     # FAASHandMapper（XHand 12D↔FAAS 32D 映射）
│   │   └── temporal_ensembler.py              # ChunkOverlapBlender（ACT 时序融合）
│   ├── datasets/                              # 数据集
│   │   ├── __init__.py                        # 空
│   │   ├── replay_buffer.py                   # ReplayBuffer（Zarr 后端）
│   │   ├── sampler.py                         # SequenceSampler（numba 滑动窗口）+ 验证/降采样 mask
│   │   ├── augmentation.py                    # 5 种 numpy 增强器 + ImageAug（torchvision）
│   │   ├── base_dataset.py                    # BaseDataset（核心逻辑+FAAS+增强+normalizer）
│   │   ├── pc_dataset.py                      # PCDataset（添加点云 normalizer）
│   │   ├── rgb_dataset.py                     # RGBDataset（仅覆盖 DEFAULT_MODALITIES）
│   │   ├── rgb_pc_dataset.py                  # RGBPCDataset（添加相机矩阵 normalizer）
│   │   └── multi_task_dataset.py              # MultiTaskDataset（多任务包装+均衡采样）
│   ├── training/                              # 训练基础设施
│   │   ├── __init__.py                        # 空
│   │   ├── build_utils.py                     # 构建工具（dataset/model/optim/scheduler/FAAS注入+校验）
│   │   ├── trainer.py                         # Trainer（主训练循环+EMA+梯度累积+3层NaN防护）
│   │   ├── workspace.py                       # TrainWorkspace（checkpoint/日志/Wandb管理）
│   │   ├── ema_model.py                       # EMAModel（逆 gamma 衰减）
│   │   ├── lr_scheduler.py                    # LR 调度器工厂
│   │   ├── logging.py                         # JsonlLogger + WandbLogger
│   │   └── eval_utils.py                      # 评测共享工具（checkpoint 加载+config 校验）
│   ├── env_runner/                            # 环境运行器
│   │   ├── __init__.py                        # 空
│   │   ├── base_runner.py                     # BaseRunner（环形观测缓冲+ACT 时序融合+单 episode 评测）
│   │   ├── sim_runner.py                      # SimRunner（dexmani_sim 集成+域随机化开关）
│   │   └── multi_task_sim_runner.py           # MultiTaskSimRunner + TaskTextSimRunner
│   ├── configs/                               # Hydra 配置
│   │   ├── dp.yaml                            # Diffusion Policy（RGB）
│   │   ├── dp3.yaml                           # DP3（PointCloud）
│   │   ├── dp3_faas.yaml                      # DP3 + FAAS（继承 dp3）
│   │   ├── maniflow.yaml                      # ManiFlow
│   │   ├── moe_dp.yaml                        # MoE Diffusion Policy
│   │   ├── multitask_dit.yaml                 # MultiTask DiT
│   │   ├── r3d.yaml                           # R3D
│   │   ├── dqrise.yaml                        # DQ-RISE（含 VQ-VAE 预训练配置段）
│   │   └── ddp/                               # DDP Overlay（6 个，复用父配置）
│   │       ├── dp.yaml, dp3_faas.yaml, dqrise.yaml,
│   │       ├── maniflow.yaml, multitask_dit.yaml, r3d.yaml
│   ├── scripts/                               # Python 辅助脚本
│   │   ├── extract_codebook.py                # VQ-VAE 码本提取+PCA 排序
│   │   ├── train_vq_hand.py                   # VQ-VAE 预训练（DQRISE Stage 1）
│   │   └── measure_vq_usage.py                # VQ 码本利用率测量
│   ├── train.py                               # 单卡训练入口（@hydra.main）
│   ├── train_ddp.py                           # 多卡 DDP 入口（mp.spawn）
│   ├── eval_best_ckpt.py                      # 离线评测入口（RoboTwin 风格，Hydra-free CLI）
│   └── smoke_test.py                          # 冒烟测试（Hydra compose API）
├── scripts/                                   # Shell 脚本
│   ├── train.sh, train_ddp.sh                 # 训练启动脚本
│   ├── eval_best_ckpt.sh                      # 评测启动脚本
│   ├── select_best_ckpt.sh                    # 最优 checkpoint 选择脚本
│   ├── train_vq_hand.sh                       # VQ-VAE 训练启动脚本
│   ├── download_pretrained.sh                 # 预训练权重下载
│   ├── clean_experiments.sh, wandb_sync.sh
├── data/pretrained/uni3d/                     # Uni3D 预训练权重
├── robot_data/                                # Zarr 数据集（8 个任务）
├── experiments/                               # 训练产出（按 policy/task/timestamp 组织）
├── docs/                                      # 文档
└── dexmani_policy.egg-info/                   # 包元数据
```

---

## 2. 模块依赖图

```
                          ┌──────────────┐
                          │   configs/   │
                          │  (YAML)      │
                          └──────┬───────┘
                                 │ Hydra instantiate
                                 ▼
┌─────────┐    ┌──────────┐    ┌──────────────┐    ┌─────────────┐
│ datasets│◄───│ training │───▶│  agents/     │───▶│ env_runner  │
│         │    │build_utils│    │  core/       │    │             │
│replay   │    │trainer   │    │  obs_encoder/│    │base_runner  │
│sampler  │    │workspace │    │  action_     │    │sim_runner   │
│augment  │    │sim_eval  │    │  decoders/   │    │multi_task   │
│normalize│    │ema_model │    │  backbone/   │    └─────────────┘
└────┬────┘    └────┬─────┘    └──────┬───────┘
     │              │                 │
     │              │                 │
     ▼              ▼                 ▼
┌──────────────────────────────────────────┐
│              common/                     │
│  normalizer  checkpoint_io  config       │
│  pytorch_util   faas_mapper              │
│  temporal_ensembler                      │
└──────────────────────────────────────────┘
```

### 依赖关系详述

| 源模块 | 依赖 | 说明 |
|--------|------|------|
| `training.trainer` | `agents.core.*`, `common.*`, `training.workspace` | 主训练循环，调用 agent.compute_loss/predict_action |
| `training.build_utils` | `datasets.*`, `agents.core.*`, `common.faas_mapper`, `common.normalizer` | 通过 Hydra 实例化 dataset 和 agent |
| `training.eval_utils` | `agents.core.base`, `common.checkpoint_io`, `env_runner.*` | checkpoint 加载、config 校验、组件构建 |
| `training.workspace` | `common.checkpoint_io`, `training.logging` | 管理 checkpoint 持久化和指标日志 |
| `agents.core.*` | `action_decoders.* + backbone.*`, `obs_encoder.*`, `common.normalizer` | Agent 组合 encoder/decoder，使用 normalizer |
| `obs_encoder.rgb.*` | `rgb.base`, `rgb.image_processor`, `rgb.geometry_processor`, `rgb.registry` | ViT 编码器继承 ViTEncoder 基类 |
| `obs_encoder.pointcloud.*` | `pointcloud.ops`, `pointcloud.registry` | PC 编码器共享 FPS/分组操作 |
| `datasets.base_dataset` | `datasets.replay_buffer`, `datasets.sampler`, `datasets.augmentation`, `common.normalizer` | 核心数据管线 |
| `env_runner.*` | `common.temporal_ensembler`, `common.pytorch_util` | 评测使用 ACT 时序融合 |

---

## 3. 核心类层级结构

### 3.1 Agent 类层级

```
nn.Module
  └── BaseAgent (base.py)                          # 抽象基类：obs_encoder+action_decoder+normalizer
        │                                           #   predict_action / compute_loss / configure_optimizer
        │
        ├── UNetDiffusionAgent (base.py)           # 便利子类：自动构建 ConditionalUnet1D + Diffusion
        │     ├── DPAgent (dp.py)                  # DPObsEncoder(RGB+StateMLP)
        │     ├── DP3Agent (dp3.py)                # DP3ObsEncoder(PointCloud+StateMLP)
        │     └── MoEAgent (moe.py)                # MoEObsEncoder(PC|RGB+MoE门控+StateMLP)
        │
        ├── DiTXFlowMatchAgent (base.py)           # 便利子类：自动构建 DiTXFlowMatch + FlowMatchWithConsistency
        │     └── ManiFlowAgent (maniflow.py)      # ManiFlowObsEncoder(PointNext patch tokenizer+StateMLP)
        │
        ├── MultiTaskAgent (multi_task.py)         # 直接继承 BaseAgent：DPObsEncoder+CLIPTextEncoder+DiT+Diff/Flow
        ├── R3DAgent (r3d.py)                      # 直接继承 BaseAgent：R3DObsEncoder+OneWayTransformer+Diffusion
        └── DQRISEAgent (dqrise.py)                # 直接继承 BaseAgent：DP3ObsEncoder+自建UNet+Diffusion+CodebookManager
```

### 3.2 Dataset 类层级

```
torch.utils.data.Dataset
  └── BaseDataset (base_dataset.py)               # 核心：Zarr加载+序列采样+增强+normalize+FAAS
        │                                           #   DEFAULT_MODALITIES = ['joint_state']
        ├── RGBDataset (rgb_dataset.py)            # DEFAULT_MODALITIES = ['joint_state', 'rgb']
        ├── PCDataset (pc_dataset.py)              # DEFAULT_MODALITIES = ['joint_state', 'point_cloud']
        │                                           #   覆盖 get_normalizer 添加 point_cloud 字段
        └── RGBPCDataset (rgb_pc_dataset.py)       # DEFAULT_MODALITIES = ['joint_state','rgb','depth','point_cloud',
            (独立)                                  #                      'camera_intrinsic','camera_extrinsic']
                                                    #   覆盖 get_normalizer 添加相机矩阵 identity normalizer
MultiTaskDataset (multi_task_dataset.py)           # 独立 Dataset 包装多个 BaseDataset，非继承关系
```

### 3.3 Action Decoder 类层级

```
nn.Module
  ├── Diffusion (diffusion.py)                    # DDIM 调度器包装（Diffusers DDIMScheduler）
  │                                                 #   预测目标：epsilon | sample | v_prediction
  │
  ├── FlowMatch (flowmatch.py)                    # 速度场预测，Euler ODE 积分
  │                                                 #   仅 MultiTaskAgent 使用
  └── FlowMatchWithConsistency (flowmatch.py)     # FlowMatch + 一致性训练（EMA 教师）
                                                    #   ManiFlow 使用，t_sample_mode × 4

辅助类：
  TimeSampler (time_sampler.py)                   # 时间采样：logit-normal/beta/cosmap/discrete
```

### 3.4 Backbone 类层级

```
nn.Module
  ├── ConditionalUnet1D (unet1d.py)              # 1D UNet + FiLM 条件化
  │                                                 #   DP/DP3/MoE/DQRISE 使用
  │
  ├── DiTDiffusion (dit.py)                      # Self-Attention + AdaLN 调制
  │                                                 #   MultiTaskAgent 使用
  │
  ├── DiTXFlowMatch (ditx.py)                     # Self-Attn + Cross-Attn（观测 token 条件）
  ├── DiTXDiffusion (ditx.py)                     #   同架构，不同输出头
  │                                                 #   ManiFlow 使用 DiTXFlowMatch
  │
  └── OneWayTransformer (one_way_transformer.py)  # Cross-Attn + 级联 mask + 分组 loss
                                                    #   R3D 使用
```

### 3.5 Obs Encoder 类层级

```
nn.Module
  ├── [RGB Encoders]
  │   ├── ViTEncoder (rgb/base.py)               # ViT 基类（forward/backproject/LoRA）
  │   │   ├── DINO (rgb/dino.py)                 # DINOv2
  │   │   ├── CLIP (rgb/clip.py)                 # CLIP Vision
  │   │   └── SigLIP (rgb/siglip.py)             # SigLIP（无 CLS token）
  │   ├── ResNet (rgb/resnet.py)                 # ResNet CNN（FrozenBN2d/GN/backproject）
  │   └── R3M (rgb/r3m.py)                      # R3M ResNet（Ego4D 预训练）
  │
  ├── [PointCloud Encoders]
  │   ├── PointNet (pointcloud/pointnet.py)      # 全局 PC 编码器
  │   ├── MultiStagePointNet (pointcloud/pointnet.py)
  │   ├── PointNextEncoder (pointcloud/pointnext.py)  # 层级 SA + IRPB
  │   ├── PointNextPatchTokenizer (pointcloud/pointnext_tokenizer.py)  # 多尺度 patch tokenizer
  │   └── Uni3DPointcloudEncoder (pointcloud/uni3d.py)  # FPS + k-NN + ViT + Fourier PE
  │
  ├── [Composite Encoders]
  │   ├── DPObsEncoder (dp.py)                    # RGB backbone + StateMLP
  │   ├── DP3ObsEncoder (dp3.py)                  # PointCloud encoder + StateMLP
  │   ├── MoEObsEncoder (moe.py)                  # RGB/PC encoder + MoE + StateMLP
  │   ├── ManiFlowObsEncoder (maniflow.py)        # PointNext tokenizer + StateMLP
  │   └── R3DObsEncoder (r3d_obs_encoder.py)      # Uni3D + StateMLP（沿 feature 维拼接）
  │
  ├── [Plugins]
  │   ├── MoE (plugins/moe.py)                    # 16 专家 top-2 路由 + 负载均衡 aux loss
  │   └── TokenCompressor (plugins/token_compressor.py)  # Cross-Attn token 压缩（未启用）
  │
  └── [Text Encoders]
      ├── CLIPTextEncoder (text/clip.py)          # 冻结 CLIP text backbone
      └── T5TextEncoder (text/t5.py)              # T5 编码器（预留，未使用）
```

### 3.6 Env Runner 类层级

```
object
  └── BaseRunner (base_runner.py)                 # 抽象基类：环形缓冲+ACT 融合+单 episode 循环
        │                                           #   make_env / get_seed_list 抽象方法
        │
        └── SimRunner (sim_runner.py)             # 具体实现：dexmani_sim 集成+域随机化开关
              └── TaskTextSimRunner (multi_task_sim_runner.py)  # 注入 task_text 到观测
                                                               #   -> 多任务评测用

MultiTaskSimRunner (multi_task_sim_runner.py)     # 独立编排器：持有 dict[str, TaskTextSimRunner]
```

---

## 4. 数据流全景

```
                    ┌─────────────────────────────────────────────────┐
                    │               Zarr 磁盘文件                      │
                    │  robot_data/<task>.zarr                         │
                    │  ├── data/joint_state  (N, 19)                  │
                    │  ├── data/action        (N, 19)                 │
                    │  ├── data/action_ee     (N, 21)                 │
                    │  ├── data/point_cloud   (N, 1024, 6)            │
                    │  ├── data/rgb           (N, H, W, 3)            │
                    │  └── meta/episode_ends  (E,)                    │
                    └──────────────┬──────────────────────────────────┘
                                   │ ReplayBuffer.copy_from_path()
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ 1. ReplayBuffer (全部 numpy float32，懒加载)                          │
│    ├── val_mask: 按 val_ratio 随机分集                                │
│    ├── train_mask: ~val_mask + max_train_episodes 降采样               │
│    └── SequenceSampler(train_mask, horizon=16, pad_before=1, pad_after=7)│
│         └── numba create_indices(): 滑动窗口生成 (N_windows, 16) 索引   │
└──────────────┬───────────────────────────────────────────────────────┘
               │ sampler.sample_sequence(idx) → numpy dict
               ▼
┌──────────────────────────────────────────────────────────────────────┐
│ 2. BaseDataset.__getitem__(idx)                                       │
│    a. sample_to_data(sample): 重组 obs dict + 拼接 aux EE             │
│    b. apply_augmentation(data): 原位 numpy 增强（首次触发时 copy）     │
│       ├── PointCoordNoiseAug  (xyz 噪声 std=0.002)                    │
│       ├── PointColorJitter    (HSV 色彩抖动)                         │
│       ├── PointColorNoiseAug  (逐通道颜色噪声)                        │
│       ├── PointDropout        (随机丢点)                             │
│       └── StateNoiseAug       (关节状态噪声 std=0.0002)              │
│    c. _preprocess_rgb_cpu(): RGB resize/crop/增强→torch tensor       │
│    d. dict_apply(ensure_tensor): numpy → torch tensor                 │
│    e. _apply_faas_mapping(): FAAS 模式: native(12D)→FAAS(32D)         │
│    → 输出: {'obs':{...}, 'action': tensor}                             │
└──────────────┬───────────────────────────────────────────────────────┘
               │ DataLoader(batch_size, num_workers, pin_memory)
               ▼
┌──────────────────────────────────────────────────────────────────────┐
│ 3. Trainer.train_one_step(batch)                                      │
│    a. batch → GPU (non_blocking=True)                                 │
│    b. model.compute_loss(batch) under bfloat16 AMP:                   │
│       ├── BaseAgent._build_cond(obs_dict):                           │
│       │   ├── normalizer.normalize(obs)          # [-1,1] 归一化     │
│       │   ├── modality_dropout (当前全为 0.0)    # 模态置零         │
│       │   ├── truncate to n_obs_steps             # [:, :2, :]       │
│       │   └── obs_encoder(obs) → cond_dict + aux                     │
│       ├── normalizer['action'].normalize(batch_action)               │
│       └── action_decoder.compute_loss(cond, normed_action)            │
│           ├── Diffusion: 加噪 → backbone(noisy, t, cond) → MSE      │
│           └── FlowMatch: 采样 t → 插值 → backbone(x_t, t, cond) → MSE│
│    c. NaN 检测 #1: raw_loss 有限性                                  │
│    d. (raw_loss / grad_accum).backward()                             │
│    e. 累积边界: apply_gradient_step()                                │
│       ├── clip_grad_norm_(max_grad_norm=1.0)                         │
│       ├── NaN 检测 #2: 扫描所有 .grad                                │
│       ├── optimizer.step() + scheduler.step() + zero_grad            │
│       └── EMA.update(model)                                          │
└──────────────┬───────────────────────────────────────────────────────┘
               │ 每个 epoch 结束
               ▼
┌──────────────────────────────────────────────────────────────────────┐
│ 4. Trainer.finish_epoch()                                             │
│    ├── Validation: validate(ema_model) → weighted val_loss            │
│    ├── Evaluation: env_runner.run(ema_model)                          │
│    │   ├── BaseRunner.run_one_episode() × eval_episodes:              │
│    │   │   ├── env.reset() → 采集 n_obs_steps 初始观测               │
│    │   │   ├── agent.predict_action(obs) → control_action(8步)       │
│    │   │   │   ├── normalizer.normalize(obs)                         │
│    │   │   │   ├── obs_encoder → cond                                │
│    │   │   │   ├── action_decoder.predict_action(cond)               │
│    │   │   │   │   └── Diffusion: 随机噪声 → DDIM 去噪(10/20步)      │
│    │   │   │   ├── normalizer['action'].unnormalize(pred)            │
│    │   │   │   └── FAAS: FAAS→native 逆转换                          │
│    │   │   ├── ChunkOverlapBlender: ACT 时序融合 (coeff=0.01)        │
│    │   │   ├── env.step(control_action) × n_action_steps             │
│    │   │   └── 检查 success_condition                                │
│    │   └── 汇总 success_rate, avg_steps, videos                      │
│    ├── Checkpoint: (epoch, step, model/ema/opt/sched+train_params)    │
│    │   ├── 原子写入 .tmp → os.replace()                              │
│    │   ├── TopK 追踪 (scores.json, 按 test_mean_score 降序)          │
│    │   └── latest symlink 更新                                       │
│    └── Logging: JsonlLogger + WandbLogger                             │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 5. 配置系统映射

### 5.1 配置 → Python 类映射表

| 配置 YAML | Agent 类 (`_target_`) | Dataset 类 | Backbone | Obs Encoder | Env Runner | 独特参数 |
|-----------|----------------------|------------|----------|-------------|------------|----------|
| `dp.yaml` | `DPAgent` | `RGBDataset` | UNet1D(256,512,1024) | DINO/CLIP/SigLIP via `rgb_backbone_name` | `SimRunner` | batch=64, eval_interval=150, epochs=600 |
| `dp3.yaml` | `DP3Agent` | `PCDataset` | UNet1D(256,512,1024) | iDP3/PointNeXT via `encoder_type` | `SimRunner` | batch=128, eval_interval=250, epochs=1000 |
| `dp3_faas.yaml` | `DP3Agent` | `PCDataset` | UNet1D(256,512,1024) | iDP3/PointNeXT | `SimRunner` | action_dim=39/41, hand_dim=32, 继承 dp3.yaml |
| `maniflow.yaml` | `ManiFlowAgent` | `PCDataset` | DiTX(12L×768) | PointNextTokenizer | `SimRunner` | betas=[0.9,0.95], wd=1e-3, flow_batch_ratio=0.75 |
| `moe_dp.yaml` | `MoEAgent` | `RGBDataset` or `PCDataset` | UNet1D(256,512,1024) | R3M+MoE(16/2) | `SimRunner` | no bfloat16, no compile, epochs=600 |
| `multitask_dit.yaml` | `MultiTaskAgent` | `MultiTaskDataset` | DiT(8L×512) | R3M/ResNet/DINO + CLIP Text | `MultiTaskSimRunner` | action_decoder_type=diffusion/flowmatch |
| `r3d.yaml` | `R3DAgent` | `PCDataset` | OneWayTransformer(4L×256) | Uni3D | `SimRunner` | use_aux_ee, dim_groups, epochs=300, eval_interval=50 |
| `dqrise.yaml` | `DQRISEAgent` | `PCDataset` | UNet1D(256,512) | iDP3 + CodebookManager | `SimRunner` | lr=3e-4, warmup=2000, prediction=epsilon, ckpt_key=val_loss |

### 5.2 Hydra 配置继承关系

```
dp3.yaml ──(defaults: [- /dp3, - _self_])──▶ dp3_faas.yaml
                                                 覆盖: action_dim=39, state_dim=39, hand_dim=32, use_faas=true

各单卡配置 ──▶ configs/ddp/<name>.yaml
                  覆盖: batch_size (每卡减小), device (cuda:rank)
```

### 5.3 配置键到模块的映射

| 配置键 | 实例化类（默认 `_target_`） | 文件 |
|--------|---------------------------|------|
| `dataset` | `BaseDataset` / `PCDataset` / `RGBDataset` / `RGBPCDataset` / `MultiTaskDataset` | `datasets/*.py` |
| `agent` | `DPAgent` / `DP3Agent` / `ManiFlowAgent` / `MoEAgent` / `MultiTaskAgent` / `R3DAgent` / `DQRISEAgent` | `agents/core/*.py` |
| `ema` | `EMAModel` | `training/ema_model.py` |
| `optimizer` | `AdamW(fused=True)` (通过 `BaseAgent.configure_optimizer`) | `agents/core/base.py` |
| `env_runner` | `SimRunner` / `MultiTaskSimRunner` | `env_runner/*.py` |
| `workspace` | `TrainWorkspace` | `training/workspace.py` |
| `workspace.wandb_cfg` | `WandbConfig` | `training/workspace.py` |
| `workspace.checkpoint_cfg` | `CheckpointConfig` | `training/workspace.py` |
| `rgb_color_aug` | `ImageAug` | `datasets/augmentation.py` |

---

## 6. 文件引用关系矩阵

### 6.1 跨包导入矩阵

**图例**: `●` = 直接导入引用，`○` = 通过 Hydra 配置间接引用，`-` = 无引用

| 被导入 →<br>导入源 ↓ | agents/<br>core | agents/<br>obs_enc | agents/<br>act_dec | agents/<br>vq_hand | datasets | training | common | env_runner | configs |
|---|---|---|---|---|---|---|---|---|---|
| **agents/core** | ● 自身 | ● dp/moe/dp3 | ● diffusion/flowmatch | - | - | - | ● normalizer | - | - |
| **agents/obs_enc** | - | ● 自身 | - | - | - | - | ● pytorch_util | - | - |
| **agents/act_dec** | - | - | ● backbone | - | - | - | - | - | - |
| **agents/vq_hand** | - | - | - | ● 自身 | - | - | - | - | - |
| **datasets** | - | - | - | - | ● replay/sampler | - | ● normalizer<br>● pytorch_util | - | - |
| **training** | ● agent 所有 | - | - | - | ● base_dataset | ● workspace | ● checkpoint_io<br>● pytorch_util<br>● faas_mapper | ● sim_runner | ○ Hydra |
| **common** | - | - | - | - | - | - | ● 自身 | - | - |
| **env_runner** | - | - | - | - | - | - | ● pytorch_util<br>● temporal_ens | - | - |
| **configs/*.yaml** | ○ _target_ | - | - | - | ○ _target_ | ○ _target_ | - | ○ _target_ | ○ 自身 |

### 6.2 关键文件间引用

| 文件 | 被引用者 |
|------|---------|
| `agents/core/base.py` | `trainer.py`, `dp.py`, `dp3.py`, `maniflow.py`, `moe.py`, `multi_task.py`, `r3d.py`, `dqrise.py`, `eval_best_ckpt.py`, `build_utils.py` |
| `datasets/base_dataset.py` | `pc_dataset.py`, `rgb_dataset.py`, `rgb_pc_dataset.py`, `build_utils.py`, `multi_task_dataset.py` |
| `training/build_utils.py` | `train.py`, `train_ddp.py`, `eval_best_ckpt.py`, `smoke_test.py` |
| `common/normalizer.py` | `base.py`, `base_dataset.py`, `build_utils.py`, `eval_utils.py` |
| `common/faas_mapper.py` | `build_utils.py`, `base.py`, `base_dataset.py` (lazy import) |
| `common/checkpoint_io.py` | `trainer.py`, `workspace.py`, `eval_utils.py` |

---

## 7. 关键设计模式

### 7.1 Strategy 模式 — Action Decoder

```python
# base.py: BaseAgent 持有抽象 action_decoder 接口
self.action_decoder = action_decoder  # Diffusion | FlowMatch | FlowMatchWithConsistency

# 统一接口，不同策略内部实现差异
action_decoder.compute_loss(cond, normed_actions)
action_decoder.predict_action(cond, template, denoise_timesteps)
```

三个 decoder 类实现了相同接口（`compute_loss` / `predict_action`），Agent 完全不知道底层是 DDIM、Euler ODE 还是 Consistency 蒸馏。

### 7.2 Template Method 模式 — BaseAgent / BaseRunner

**BaseAgent 模板**:
- `compute_loss()` 定义了固定流程：`_build_cond → normalize(action) → action_decoder.compute_loss`
- 子类覆盖 `_get_dim_groups()` 注入分组 loss（R3DAgent）
- 子类覆盖 `predict_action()` / `predict_action_from_cond()` 注入 FAAS 转换
- 子类覆盖 `get_optim_param_groups()` 注入自定义参数分组

**BaseRunner 模板**:
- `run_one_episode()` 定义了固定评测流程（reset → rollout → check success）
- 子类覆盖 `make_env()` 和 `get_seed_list()` 实现环境差异

### 7.3 Factory 模式 — Registry

```python
# obs_encoder/rgb/registry.py
def build_backbone(name: BackboneName, config=None) -> Tuple[nn.Module, ImageProcessor]:
    # 根据字符串名创建对应的 backbone 实例

# obs_encoder/pointcloud/registry.py
def build_pc_global_encoder(encoder_type, pc_dim, config=None) -> nn.Module:
def build_pc_patch_tokenizer(tokenizer_type, pc_dim, config=None) -> nn.Module:
```

配合 Hydra 的 `_target_` 机制，整个对象图通过 YAML 配置声明式构建，无需工厂代码感知具体类。

### 7.4 Builder 模式 — build_utils.py

```python
# training/build_utils.py 中的构建函数链
dataset, normalizer = build_dataset_and_normalizer(cfg)
model, ema_model, ema_updater = build_model_and_ema(cfg, device, normalizer)
optimizer, scheduler = build_optimizer_and_scheduler(cfg, model, batches_per_epoch)
```

所有构建逻辑集中在一个文件中，统一注入 FAAS、加载 normalizer、分配设备。

### 7.5 Observer 模式 — Checkpoint TopK 追踪

```python
# checkpoint_io.py: TopKCheckpointTracker
# 每保存一个 checkpoint 时调用 tracker.save(path, score)
# tracker 自动维护 scores.json，按阈值淘汰
# workspace.save_topk() 解耦保存逻辑与淘汰策略
```

### 7.6 Composite 模式 — MultiTaskDataset / MultiTaskSimRunner

```python
# MultiTaskDataset 持有 List[BaseDataset]
# MultiTaskSimRunner 持有 Dict[str, TaskTextSimRunner]
# 对外暴露与单一 dataset/runner 相同的接口
```

### 7.7 Proxy 模式 — EMAModel

```python
# training/ema_model.py
# EMAModel 持有模型引用，提供 step() 更新 EMA 权重
# 对外表现为普通模型（可直接调用 forward）
```

### 7.8 依赖注入 — FAAS

```python
# build_utils.py: inject_faas_into_agent(agent, cfg)
# 通过 setattr 注入 use_faas, tcp_dim, faas_mapper 属性
# Agent 内部通过 getattr(self, 'use_faas', False) 条件行为
# 零侵入式扩展 — 所有 Agent 不加代码即可支持 FAAS
```

---

## 8. 外部依赖

### 8.1 深度学习框架

| 库 | 用途 |
|----|------|
| `torch` (PyTorch) | 核心框架：自动微分、AMP、DDP、compile |
| `torchvision` | ResNet backbone、图像 transforms（resize/crop/ColorJitter） |
| `einops` (`rearrange`, `reduce`, `repeat`, `pack`, `unpack`) | 张量重塑，广泛用于 VQ 模块、UNet、Transformer |
| `timm` | Uni3D 中的 ViT 模型加载；DiT/DiTX 中的 `Mlp`、`use_fused_attn`、`RmsNorm` |
| `torch3d` (PyTorch3D) | `pytorch3d.ops.knn_points`、`ball_query` — 点云 k-NN 搜索 |

### 8.2 Transformers / 预训练模型

| 库 | 用途 |
|----|------|
| `transformers` (HuggingFace) | DINOv2 (`AutoModel`)、CLIP (`CLIPVisionModel`, `CLIPTextModel`)、SigLIP (`SiglipVisionModel`)、T5 (`T5EncoderModel`) |
| `diffusers` | `DDIMScheduler`（扩散调度）+ `SchedulerType`（LR 调度器类型定义） |

### 8.3 数据处理 / 中间件

| 库 | 用途 |
|----|------|
| `numpy` | 核心数组操作，数据增强（原位） |
| `numba` | `create_indices()` JIT 编译，加速 SequenceSampler 滑动窗口索引生成 |
| `zarr` | 高效磁盘存储（ReplayBuffer 后端），支持分块读取 |
| `scikit-learn` (`sklearn.decomposition.PCA`) | DQ-RISE CodebookManager 中的 PCA 排序 |
| `omegaconf` / `hydra` | 配置系统（YAML → 结构化配置 + 实例化） |

### 8.4 仿真 / 环境

| 库 | 用途 |
|----|------|
| `dexmani_sim` | 灵巧手操作仿真环境（Isaac Sim 封装） |

### 8.5 日志 / 监控

| 库 | 用途 |
|----|------|
| `wandb` | 训练指标跟踪、视频记录、config 快照 |
| `termcolor` | 终端彩色输出（`cprint`） |
| `tqdm` | 训练进度条 |
| `imageio` | 评测视频 GIF 写入 |

### 8.6 工具 / 其他

| 库 | 用途 |
|----|------|
| `dataclasses` | `TrainLoopConfig`, `TrainCheckpoint`, `CheckpointConfig`, `WandbConfig` |
| `pathlib` | 路径操作 |
| `json` | `scores.json` 持久化、配置解析 |
| `itertools` | VQ CodebookManager 中的码本组合遍历 |
| `re` | 从文件名解析 epoch/step/score |
| `functools` | `partial`（VQ 模块） |
| `atexit` | 日志器 close 注册 |
| `multiprocessing` | MultiTaskDataset 并行 normalizer 拟合 |
