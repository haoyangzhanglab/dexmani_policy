# DexMani_Policy 项目架构

> 本文描述仓库的**稳定系统架构、核心接口与设计边界**，不维护当前 Policy 的 layer 数、hidden dim、batch size、NFE、参数量或 Policy 数量等易变 recipe。
>
> **Source of Truth**：
> - 已有实验：实验目录保存的 resolved `config.yaml` + checkpoint contract。
> - 当前 Policy：`dexmani_policy/configs/*.yaml` → `agent._target_` → Python 实现。
> - 稳定系统 contract：本文 + 对应公共实现。
> - 若本文与可执行 config/code 冲突，以 config/code 为准。

---

## 1. Repository Boundaries

```text
dexmani_policy/
├── configs/            # Hydra experiment / Policy 配置；ddp/ 为可选多卡 overlay
├── agents/             # Agent、Observation Encoder、Action Decoder、Backbone
├── datasets/           # Zarr 加载、序列采样、增强、normalization
├── training/           # 构建、Trainer、EMA、resume、checkpoint/eval utilities
├── env_runner/         # Simulation rollout boundary
├── common/             # config/checkpoint/normalizer 等共享 contract
├── deployment/         # 持久 deployment artifact 的 export/restore/runtime 边界
├── tools/              # 离线研究工具
├── train.py            # 单进程训练入口
├── train_ddp.py        # DDP 训练入口
├── select_best_ckpt.py # 离线 checkpoint selection
├── eval_best_ckpt.py   # held-out 仿真评测
├── record_demo.py      # demo 录制
└── smoke_test.py       # config / integration validation

scripts/
├── training/           # 本地训练 shell 入口
├── eval/               # selection / eval / demo orchestration
├── remote/             # 远程同步与训练
└── utils/              # 辅助运维脚本
```

具体 Policy、backbone、DDP overlay 和工具集合以当前目录与 Hydra config 为准，不在本文维护静态清单。

---

## 2. System Architecture

DexMani_Policy 将 **Training、Model Selection、Final Evaluation、Deployment** 明确拆开。训练过程不负责在线仿真评测，也不根据 validation metric 自动选择 best model。

### 2.1 Training

```text
Resolved Hydra Config
        │
        ├───────────────┐
        ▼               ▼
     Dataset          Agent
        │               ▲
        ├── get_normalizer ──► Normalizer ── load into Agent
        │               │
        ▼               ├── Observation Encoder
    DataLoader          └── Action Decoder / Backbone
        │               │
        └───────┬───────┘
                ▼
             Trainer
                │
                ├── compute loss / backward
                ├── gradient accumulation
                ├── gradient clipping + non-finite protection
                ├── optimizer.step
                ├── scheduler.step
                ├── EMA update
                ├── logging
                └── progress milestone checkpoint
```

训练入口由 `train.py` / `train_ddp.py` 协调多个构建模块：

```text
training/build_utils.py
    ├── build_dataset_and_normalizer
    ├── build_model_and_ema
    └── build_optimizer_and_scheduler

training/resume.py
    ├── build_train_loader
    └── build_resume_contract

                ↓
            Trainer.train
```

`Trainer` 是 **step-driven**：`global_step` 表示 optimizer step；epoch 只是 DataLoader 数据遍历单位。当前训练生命周期中没有 `validate()` 或 `env_runner.run()`。

训练 checkpoint 按 progress milestone 保存；best checkpoint 的选择属于后续离线评测流程。

### 2.2 Offline Model Selection

```text
milestone checkpoints
        │
        ▼
select_best_ckpt.py
        │
        ├── paired selection seeds
        ├── candidate comparison
        └── optional exact-tie batch
        │
        ▼
best_ckpt.json
```

这样把 optimization 与 simulator-based model selection 解耦：训练只负责产生可恢复的 milestone；selection 负责在统一 protocol 下比较候选模型。

selection 的 seed、ranking 和异常语义详见 [`仿真评测机制.md`](./仿真评测机制.md)。

### 2.3 Held-out Final Evaluation

```text
experiment/config.yaml
        +
best_ckpt.json / explicit checkpoint
        │
        ▼
eval_best_ckpt.py
        │
        ├── eval config validation
        ├── checkpoint contract validation
        ├── raw / EMA state restore
        └── held-out seed selection
        │
        ▼
EnvRunner
        │
        ▼
simulation rollout + metrics
```

selection seeds 与 `best` final-eval seeds 分离，避免使用 checkpoint-selection episodes 直接报告最终性能。

### 2.4 Deployment Boundary

```text
resolved experiment config
        +
selected checkpoint
        +
dataset semantics
        │
        ▼
deployment/export.py
        │
        ▼
canonical deployment artifact
        │
        ├── deployment/restore.py
        ├── deployment/runtime.py
        └── deployment/qualify.py
```

`deployment/` 拥有**持久模型 artifact contract**。当前 canonical artifact 使用 `dexmani.deployment` format，并把 inference 配置、weights 与 observation/data semantics 固化到一个严格可解析的边界中。

稳定语义包括：

- `data_contract.observation_fields` 是有序的 raw model-input field mapping；每个 field 记录 name、raw shape、dtype 与 semantics。
- export 会验证这些 field 与已实例化 ObsEncoder 实际消费的 observation fields 一致，避免声明了数据但模型没有真实 consumer。
- RGB artifact contract 会记录从 raw HWC `uint8` RGB 到 deterministic validation spatial preprocessing，再到 vision `ImageProcessor` normalization 的完整链路。
- inference contract 保存 action/window 相关字段以及评测时需要的 denoise-step 配置；restore 时按 schema 严格解析。
- `restore.py` 负责 strict restore；`qualify.py` 用 direct/export parity 检查导出前后的 prediction 一致性。

它**不负责 Real robot IO**。Real-side camera、robot SDK、control loop 等硬件适配应位于该 artifact boundary 之外。

更细的 schema 字段以 `deployment/contract.py`、`deployment/export.py` 和 `deployment/restore.py` 为准，不在本文复制易变的传感器 metadata 常量。

---

## 3. Core Contracts

### 3.1 Dataset Contract

数据链路：

```text
Zarr on disk
   │
   ▼
ReplayBuffer.copy_from_path
   │  selected arrays materialized into NumPy memory
   ▼
SequenceSampler
   │
   ▼
BaseDataset.__getitem__
   ├── sample_to_data
   ├── configured augmentation
   ├── optional RGB CPU preprocessing
   └── NumPy → torch.Tensor
   │
   ▼
{"obs": {...}, "action": Tensor}
```

关键语义：

- `ReplayBuffer.copy_from_path()` 当前会对所选 Zarr array 执行完整读取，不是 lazy on-demand Zarr access。
- floating arrays 如果不是 `float32` 会转换为 `float32`；`uint8` 等非浮点 dtype 保留。
- `SequenceSampler` 负责 sequence length、padding 与 episode mask 对应的采样窗口。
- augmentation 属于 dataset config，不是固定架构常量。
- RGB training path 可以执行随机 crop / color augmentation；validation/evaluation path 使用确定性的 resize / center crop。

Normalizer 由 Dataset 拟合并加载进 Agent：

- 基础数据集负责 `joint_state` / `action`。
- Point-cloud dataset 还拟合 `point_cloud`。
- RGB 不走 `LinearNormalizer`；RGB spatial preprocessing 与 vision encoder processor 共同构成图像预处理链。

当前 `BaseDataset` / `PCDataset` 的 normalizer fitting 直接读取 `ReplayBuffer` 中的完整数组，并**不会应用 `train_mask`**。因此如果某个实验把 `val_ratio` 设为非零，这不是严格的 train-only normalization；不要把“全 replay-buffer 拟合”误写成与 train-split 拟合等价。

### 3.2 Agent Contract

`BaseAgent` 是 Policy 的主要公共边界：

```text
raw observation
     │
     ▼
preprocess
  ├── policy normalizer
  ├── optional modality dropout (training only)
  └── truncate to n_obs_steps / flatten B×T
     │
     ▼
Observation Encoder
     │
     ▼
conditioning
     │
     ├──────────────► ActionDecoder.compute_loss   (training)
     │
     └──────────────► ActionDecoder.predict_action (inference)
```

稳定接口包括：

```python
compute_loss(batch, **kwargs)
predict_action(obs_dict, denoise_timesteps=None)
predict_action_from_cond(cond, denoise_timesteps=None)
configure_optimizer(...)
control_action_dim
```

`BaseAgent` 的默认 inference action slicing：

```python
start = n_obs_steps - 1
control_action = pred[:, start : start + n_action_steps]
tail = pred[:, start + n_action_steps :]
```

Policy 可以 override `predict_action_from_cond()` 处理内部 action representation。若包含 auxiliary prediction dimensions，可通过 `control_action_dim` 将 environment-facing action 与 model output 维度解耦。

### 3.3 Observation Encoder Contract

ObsEncoder 的职责是把经过公共 preprocessing 的观测转换成 Action Decoder 所需的 conditioning representation。

不同 Policy 可以采用不同 representation family，例如：

- RGB global / dense feature；
- Point-cloud global feature；
- dense 3D token / patch token；
- geometry-aware memory；
- proprioceptive / text conditioning；
- multi-modal fused tokens。

具体 token 数、feature dim、position encoding、attention 结构从当前 Policy config 与实现读取，不在全局文档固定。

### 3.4 Action Decoder Contract

共享语义接口：

```python
compute_loss(cond, actions, **kwargs)
predict_action(cond, action_template, denoise_timesteps=None)
```

Decoder 可以实现不同生成方案，例如：

- Diffusion / DDIM；
- Flow Matching / Rectified-Flow variants + ODE integration；
- consistency-enhanced Flow；
- Policy-specific action representation / solver。

因此需要明确区分：

```text
Observation Representation
Policy Architecture
Action Representation
Training Objective
Inference Algorithm
```

“使用 Transformer”或“使用 Flow Matching”本身不足以完整描述一个 Policy。

### 3.5 EnvRunner Contract

单任务 runner 的稳定关系：

```text
BaseRunner
   └── SimRunner
         └── TaskTextSimRunner (task-text injection)
```

`BaseRunner` 负责：

- temporal observation buffer；
- inference batch construction；
- action chunk execution；
- episode success / step collection；
- fail-fast evaluation semantics；
- optional video handling。

`MultiTaskSimRunner` 是**独立 orchestrator**，组合多个 `TaskTextSimRunner`，不是 `BaseRunner` 子类。它负责 task-specific seed mapping、逐任务执行以及 macro/micro aggregation。

### 3.6 Checkpoint / Resume Contract

训练 checkpoint 使用严格 schema 保存训练状态，包括：

```text
model weights
EMA weights (if enabled)
optimizer state
scheduler state
resume contract
EMA updater state
RNG states
training position
```

Resume 与 inference 都不应只依赖 state-dict shape “碰巧兼容”。

Inference restore 会比较 checkpoint 中的 `resume_contract.agent` 与当前实例通过 `build_agent_contract()` 构造的 agent contract。该 contract 覆盖 observation/action window、action semantics 与 Policy-specific control dimensions 等字段。

设计原则：

> **Checkpoint compatibility 是 semantic contract，而不只是 parameter-shape compatibility。**

---

## 4. End-to-End Data Flow

### 4.1 Training Path

```text
Zarr
 │
 ▼
ReplayBuffer (RAM)
 │
 ▼
SequenceSampler
 │
 ▼
Dataset.__getitem__
 │
 ├── observation augmentation
 ├── RGB preprocessing (if applicable)
 └── action sequence
 │
 ▼
DataLoader
 │
 ▼
Trainer.train_one_step
 │
 ▼
Agent.compute_loss
 │
 ├── obs normalization / preprocessing
 ├── ObsEncoder → condition
 ├── action normalization
 └── ActionDecoder training objective
 │
 ▼
backward
 │
 ▼
accumulation boundary
 │
 ├── gradient clipping / non-finite validation
 ├── optimizer.step
 ├── scheduler.step
 └── EMA update
```

具体 non-finite 检查与 clipping 的内部顺序取决于 `fast_grad_finite_check` 路径；架构 contract 是**在 optimizer state 更新前拒绝 non-finite gradient**。

训练达到 milestone 时保存 checkpoint；不会在 epoch 结束后自动执行 simulation evaluation。

### 4.2 Inference / Evaluation Path

```text
environment observation
       │
       ▼
EnvRunner temporal buffer
       │
       ▼
stacking + modality-specific validation preprocessing
       │
       ▼
Agent.predict_action
       │
       ├── policy normalization
       ├── ObsEncoder
       ├── generative sampling / ODE solve
       └── action unnormalize
       │
       ▼
control_action slice
       │
       ▼
env.step × chunk
```

生成起点、solver、NFE、prediction target 等由具体 Action Decoder 决定。

---

## 5. Policy Architecture Taxonomy

本文不维护当前 Policy catalog，但可以用以下维度理解和比较仓库中的 Policy。

### 5.1 Observation Representation

```text
RGB / Point Cloud / Proprioception / Text
        ↓
global feature / dense feature / patch token / geometry memory
```

### 5.2 Conditioning Architecture

常见模式包括：

- global concatenation / FiLM conditioning；
- action-to-observation cross-attention；
- multimodal joint attention；
- structured / selective conditioning。

### 5.3 Action Representation

常见模式包括：

- temporal action sequence `(B, T, Da)`；
- structural/action-dimension tokens；
- reduced or quantized hand representation；
- primary control dimensions + auxiliary prediction dimensions。

### 5.4 Training Objective

常见目标包括：

- diffusion noise / sample / velocity prediction；
- Flow Matching velocity regression；
- consistency / teacher-assisted objective；
- policy-specific auxiliary losses。

### 5.5 Inference Algorithm

必须与 Training Objective 分开描述，例如：

- DDIM iterative denoising；
- Euler ODE integration；
- midpoint ODE integration；
- Policy-specific few-step generation path（仅在对应实现存在时）。

Flow Matching 并不天然等价于 one-step inference；NFE 与 solver 是独立的 inference design choice。

---

## 6. Hydra Configuration Model

Hydra `_target_` 是当前实现的入口，而不是 config 文件名或文档中的静态 mapping。

```text
cfg.dataset._target_
    → Dataset

cfg.agent._target_
    → Agent
       ├── Observation Encoder
       └── Action Decoder / Backbone

cfg.env_runner._target_
    → EnvRunner

cfg.workspace._target_
    → TrainWorkspace

cfg.ema._target_
    → EMA updater
```

处理或 review 一个 Policy 时，按以下路径追踪：

```text
resolved config
→ agent._target_
→ Agent.__init__
→ obs_encoder
→ backbone / action_decoder
→ compute_loss
→ predict_action
→ environment-facing control_action
```

不要假设 config 名、Python module 名和 Agent class 名相同。

---

## 7. Extension Workflow

新增或重构 Policy 的推荐路径：

```text
Problem / Hypothesis
      │
      ▼
Hydra config
      │
      ▼
Agent construction
      │
      ├── Observation Representation
      ├── Policy Architecture
      ├── Action Representation
      ├── Training Objective
      └── Inference Algorithm
      │
      ▼
config-only smoke
      │
      ▼
full integration smoke
      │
      ▼
training
      │
      ▼
fixed-protocol checkpoint selection
      │
      ▼
held-out evaluation
```

复用共享组件前先确认语义一致；Policy-specific 逻辑优先保持局部化。

---

## 8. Entry Point Map

| 目标 | 入口 |
|---|---|
| 单卡训练 | `dexmani_policy/train.py` / `scripts/training/train.sh` |
| DDP 训练 | `dexmani_policy/train_ddp.py` / `scripts/training/train_ddp.sh` |
| Dataset / Agent / Optimizer 构建 | `dexmani_policy/training/build_utils.py` |
| Train DataLoader / Resume contract | `dexmani_policy/training/resume.py` |
| 训练循环 | `dexmani_policy/training/trainer.py` |
| Checkpoint schema | `dexmani_policy/common/checkpoint_io.py` |
| Agent contract | `dexmani_policy/agents/core/base.py` |
| Checkpoint selection | `dexmani_policy/select_best_ckpt.py` |
| Final evaluation | `dexmani_policy/eval_best_ckpt.py` |
| Eval shared contract | `dexmani_policy/training/eval_utils.py` |
| Simulation rollout | `dexmani_policy/env_runner/` |
| Deployment artifact | `dexmani_policy/deployment/` |
| Config/integration validation | `dexmani_policy/smoke_test.py` |

---

## 9. Documentation Maintenance Policy

### 需要同步更新本文

当以下稳定系统行为变化时更新：

- public CLI / repository entry point；
- Training lifecycle；
- Dataset / Agent / Decoder / EnvRunner 公共 contract；
- checkpoint / resume schema；
- model-selection 或 evaluation boundary；
- deployment artifact boundary。

### 不需要同步更新本文

正常研究迭代中的以下变化应留在 config / code / experiment snapshot：

- layer 数、hidden dim、head 数；
- encoder/backbone 的当前选择；
- batch size、LR、weight decay、warmup；
- NFE、solver 的当前 recipe；
- 参数量；
- 当前 Policy / task / DDP overlay 数量；
- 实验结果与结论。

统一原则：

> **Docs 记录 architecture + contract + rationale；Config 记录 current recipe；Code 记录 executable mechanism；Experiment snapshot 记录 historical fact。**
